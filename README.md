# 🔢 Проект: Сервис расчета квадратов (SQR Service)

## 📝 Описание проекта
Приложение рассчитывает количество чисел в диапазоне от 10 до 99, квадраты которых попадают в заданный пользователем интервал (границы диапазона).

---

## 🛠 Технологический стек
* **Язык программирования:** Java 11
* **Архитектура:** Service-oriented approach (SqrService)
* **Среда разработки:** IntelliJ IDEA

---

## ⚙️ Условия задачи
* **Логика:** перебор чисел от 10 до 99 и проверка условия: `квадрат >= нижняя_граница` и `квадрат <= верхняя_граница`.
* **Реализация:** использование цикла `for` и счетчика совпадений.

---

## 🚀 Как запустить проект
1. Склонируйте репозиторий:
   `git clone https://github.com/Yangerlol/sqr-service2.3.git`
2. Откройте проект в IntelliJ IDEA.
3. Запустите класс `Main` для проверки работы сервиса.

---

## 📊 Пример реализации кода

**SqrService.java**
```
public class SqrService {
    public int calculate(int lowLimit, int highLimit) {
        int count = 0;
        for (int i = 10; i <= 99; i++) {
            if (i * i >= lowLimit && i * i <= highLimit) {
                count++;
            }
        }
        return count;
    }
}
```
Main.java

```
public class Main {
    public static void main(String[] args) {
        SqrService service = new SqrService();
        int result = service.calculate(200, 300);
        System.out.println("Количество найденных чисел: " + result);
    }
}
```
## ✅ Результаты работы
В ходе выполнения проекта реализовано:

Использование циклов: реализован цикл for с фиксированным диапазоном от 10 до 99.

Условные операторы: применена логическая проверка попадания квадрата числа в заданный интервал.

Выделение логики: расчет полностью изолирован в методе calculate сервисного класса.
